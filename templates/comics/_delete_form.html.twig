<form method="post" action="{{ path('app_comics_delete', {'id': comic.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comic.id) }}">
    <button class="btn">Delete</button>
</form>
