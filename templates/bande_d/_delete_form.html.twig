<form method="post" action="{{ path('app_bande_d_delete', {'id': bande_d.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bande_d.id) }}">
    <button class="btn">Delete</button>
</form>
