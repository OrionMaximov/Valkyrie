{% extends 'base.html.twig' %} {% block stylesheets %}
{{ encore_entry_link_tags("app") }}
<link rel="stylesheet" href="{{ asset('assets/css/card.css') }}" />

{% endblock %} {% block title %}Mangas index{% endblock %} {% block body %}
<div class="bookAlignItem">
  <h2>Mangas</h2>


    <div class="table">
     
        {% for manga in mangas %} 
        <div class="card text-white bg-dark mb-3" style="max-width: 120rem;">
        <div class="card-header">
          <h3>{{ manga.titre }}</h3></div>
          <div class="card-body">
          <h4>{{ manga.auteur }}</h4>
          <h4>Tome:{{ manga.tome }}</h4>
          <h4>Serie:{{ manga.serie }}</h4>
          <h4>Prix:{{ manga.prix }}</h4>
          <h5>
            <a href="{{ path('app_mangas_show', { id: manga.id }) }}"
              >+ d'infos</a
            >
          </h5>
        </div>
        </div>
        {% else %}
        <tr>
          <p colspan="11">Y'a rien, achète autre chose</p>
        </tr>
        {% endfor %}
    
    </div>
  
</div>

{% endblock %}
